import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    title: {
      fontSize: 31,
      fontWeight: 'bold',
      flex:1
      
    },
    container:{
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
    }
  });